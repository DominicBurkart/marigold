PR #115 CI re-trigger marker — 2026-05-01

The push workflows ([push]-triggered: Tests, style hooks, builds, Benches)
did not produce check runs on commit 7942aace4709af34e61f355c14cec21b2dd20537 —
only CodeQL/Analyze ran. This commit re-triggers the missing workflows.

Same symptom observed on PR #140 (head a3e2b40c) pushed in the same window,
suggesting transient GitHub Actions scheduling/queue behavior on rapid
concurrent pushes. The implementation itself is verified by prior local runs
(see PR comments: cargo test -p marigold-grammar/tests, fmt, clippy all clean).

Session: claude/sdk
