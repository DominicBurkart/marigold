fn doubled_plus_ten(i: i32) -> i32 {
    (i * 2) + 10
}

digits = range(0, 10)

digits
    .filter(is_even)
    .return

odd_digits = digits
    .filter(is_odd)

odd_digits
    .map(doubled_plus_ten)
    .return